<h2 id="bookmarks"><em> <%= @bookmark.title %> </em></h2>

<%= render partial: 'form', locals: {topic: @bookmark.topic, bookmark: @bookmark } %>

<%= link_to 'Back', :back, class: 'btn btn-xs btn-info'%>
